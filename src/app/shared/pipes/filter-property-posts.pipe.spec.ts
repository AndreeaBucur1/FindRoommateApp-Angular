import { FilterPropertyPostsPipe } from './filter-property-posts.pipe';

describe('FilterPropertyPostsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPropertyPostsPipe();
    expect(pipe).toBeTruthy();
  });
});
