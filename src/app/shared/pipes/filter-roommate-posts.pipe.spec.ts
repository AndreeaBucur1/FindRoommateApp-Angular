import { FilterRoommatePostsPipe } from './filter-roommate-posts.pipe';

describe('FilterRoommatePostsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoommatePostsPipe();
    expect(pipe).toBeTruthy();
  });
});
